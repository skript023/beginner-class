//Function terdapat 2 tipe, function biasa dan function arrow atau anonimus function atau biasa saya sebut dengan lambda function karena biasanya function arrow tidak perlu deklarasi nama atau bisa langsung

function fungsi(iParam0, iParam1)
{
    console.log(`Fungsi dengan parameter, nilai parameter 1 : ${iParam0} parameter 2 ${iParam1}`);
}

arrow_function = (iParam0, iParam1) => { console.log(`Fungsi dengan parameter, nilai parameter 1 : ${iParam0} parameter 2 ${iParam1}`); }